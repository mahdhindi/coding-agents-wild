# ------------------------------
# Replication outputs (DO NOT COMMIT)
# ------------------------------
# Generated datasets / derived artifacts (can contain secrets or huge files)
data/derived/
data/derived/**

# If you also generate raw cached downloads locally, ignore them too
data/raw/
data/raw/**

# Ignore generated tables/exports anywhere (optional but recommended)
*.csv
*.xlsx

# Jupyter / temporary exports
*.ipynb_checkpoints/
.ipynb_checkpoints/

# OS / editor junk
.DS_Store
Thumbs.db

# Virtual env (keep both forms; you already ignore .venv but this makes it explicit)
.venv/

# === Replication outputs / local data (DO NOT COMMIT) ===
data/raw/
data/derived/
data/cache/
*.parquet
*.feather

# Large / generated exports
*.csv
*.xlsx
*.xlsm

# If you want to keep small example CSVs, put them in /samples and unignore:
!samples/
!samples/**

# Local configs / tokens
config/local.yaml
config/*.local.yaml
*.token
*.key
*.pem
